import { React } from "react";
import { Text, View } from "react-native";

export default function List({navigation}) {
    
    return (
        <View>
            <Text>Hey List works!</Text>
        </View>
    );
}